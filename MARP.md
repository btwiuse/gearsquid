# Gear Squid

```
# Setup Squid CLI
npm install -g @subsquid/cli

# Auth
sqd auth --key <TOKEN>

# Deploy your squid
sqd squid:release gearsquid@<squid_version> --source <github_url>#<github_branch>
sqd squid:release gearsquid@v0 --source https://github.com/btwiuse/gearsquid#main
```
